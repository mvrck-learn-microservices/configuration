app:
  service:
    cordelia:
      baseUrl: cordelia:8081
      clientId: nightfury
      secret: secret
      grantType: password
redis:
  config:
    cache:
      host: redis
      port: 6379
      password: "SevenEightTwo782"
server:
  port: 8082
  servlet:
    context-path: /api/enclave